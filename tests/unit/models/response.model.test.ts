import ResponseModel from "../../../src/models/response.model";

describe("Response model", () => {
    it("Should do some funky stuff", () => {
        const reponseModel = new ResponseModel();

        expect(true).toEqual(true);
    });
});